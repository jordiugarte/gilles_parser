!! Unclosed comment

LET Random_Gigo BE
  IN(a):
  IN(b):
  IN(c):
  WHILE {0 < a} REPEAT
    WHILE {0 < b} REPEAT
      WHILE {0 < c} REPEAT
        d = a+b+c:
        IF {d == 0} THEN
          OUT(d):
        else $ This will be a variable since it's lower case and we think it's
          OUT(b):
        END:
      END:
      e = a+b:
      IF {e == 0} THEN
          OUT(e):
      ELSE
          OUT(a):
      END:
    END:
  END:
  OUT(a):
END
