!! Euclid's algorithm !!

LET Euclid BE
  IN(a):
  IN(b):
  WHILE {0 < b} REPEAT
    c = b:
    WHILE {b <= a} REPEAT     $ Computation of modulo
      a = a-b:
    END:
    b = a:
    a = c:
  END:
  OUT(a):
END
